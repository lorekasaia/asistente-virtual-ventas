# Fichero de requerimientos para el Agente Comercial Batia
# Instalar todas las dependencias con: pip install -r requirements.txt

# --- Core AI & Web Framework ---
# Kit de desarrollo de agentes de Google y sus dependencias de IA
google-adk>=1.5.0
# Framework para crear la API y la interfaz web
fastapi
# Servidor web para ejecutar FastAPI
uvicorn[standard]

# --- Conectividad y Datos ---
python-dotenv         # Para cargar variables de entorno desde el archivo .env
pandas                # Para manipulación y análisis de datos (ej. resultados de SQL)
SQLAlchemy            # ORM para interactuar con la base de datos de forma segura
cloud-sql-python-connector[pg8000] # Conector oficial para Cloud SQL con el driver de PostgreSQL

# --- Utilidades Adicionales ---
matplotlib            # Para generar los gráficos que se muestran en el chat
python-multipart      # Necesario para que FastAPI maneje formularios y subidas
requests              # Dependencia interna necesaria para las librerías de Google Cloud y GenAI
openpyxl              # Para exportar DataFrames a formato Excel (.xlsx)
PyPDF2                # Para leer y extraer texto de archivos PDF reales
python-docx           # Para leer documentos de Word (.docx)
Pillow                # Para procesar y abrir imágenes
pytesseract           # Motor OCR para extraer texto de imágenes